// import { PersistedState } from "redux-persist/lib/types";

// import * as authSlice from "features/auth/authSlice";
// import * as reportSlice from "features/report/reportSlice";
// import * as resumeSlice from "features/resume/resumeSlice";
// import * as userSlice from "features/user/userSlice";

// export const MIGRATION_VERSION = 1;

// export const migrations = {
//   [MIGRATION_VERSION]: (state: any): PersistedState => {
//     if (state)
//       return {
//         ...state,
//         auth: authSlice.initialState,
//         resume: resumeSlice.initialState,
//         report: reportSlice.initialState,
//         user: userSlice.initialState,
//       };
//   },
// };

export default {};
